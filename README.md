# Certifying the kinetics of machine-learned interatomic potentials from single-point energies

Code and data for the paper of the same name (Xiyao Yu, Nanyang Technological University).

Every number and every figure in the paper and its Supplemental Material can be reproduced from what is here.
Raw molecular-dynamics trajectories are not deposited: they run to several gigabytes and are regenerated by the
notebooks in `colab/` with the seeds the scripts carry. The quantities derived from them (hop events,
committor increments, per-frame energy errors and rates) are all included.

## Layout

The tree is the one the scripts expect, so every program here runs unmodified from the directory it sits in.

| | |
|---|---|
| `*.py` (top level) | one- and two-dimensional numerical experiments: exact generators, brackets, friction, diffusion, acquisition. Run from the top level; they write into `data/` |
| `ala2/` | alanine dipeptide: teacher and student dynamics, force errors, predictions, acquisition, and the student networks (`student_*.pt`) |
| `li3ocl/` | Li<sub>3</sub>OCl: hop analysis, defect states, leverage density, certificates |
| `colab/` | the production runs as executed (Google Colab notebooks), the batched molecular-dynamics engine, the MACE teacher and students, and equilibrium frames |
| `research_extensions/` | response geometry |
| `dft/` | density-functional single points: inputs, energies (`E0`, extrapolated to zero smearing), magnetic moments and structures of the hop-path and equilibrium frames of the teacher; `paired/` holds the basin frames of the pairing test, and `excluded_fused_pairs.txt` the hop frames with a fused lithium pair, which were not computed |
| `data/` | analysis outputs, including the predictions computed from reference data (`*_predicted*`, `li3ocl_pred_*`) |
| `figs/data/` | the numerical values behind every figure |

## Figures

Value files in `figs/data/` keep the names of the figure sources, which do not follow the published
numbering. This table is the correspondence.

| figure | subject | values in `figs/data/` | code |
|---|---|---|---|
| 1 | equal force errors, different rates: double well and alanine dipeptide | `fig1_potentials.csv`, `fig8_nu_pts.csv` | `doublewell_check.py`, `ala2/path_ala2.py` |
| 2 | workflow | none (diagram) | |
| 3 | local saddle data do not determine the rate | `fig14_*.csv` | `matched_saddle.py`, `matched_saddle_2d.py` |
| 4 | condition number of a rate, and predictors | `fig3_condition.csv`, `fig3_scatter.csv` | `doublewell_check.py` |
| 5 | flow coordinates, series and parallel laws | `fig9_slice_cells.csv`, `fig9_tube_cells.csv` | `tube_bounds.py`, `path_bound.py` |
| 6 | alanine dipeptide, designed errors | `fig5_ala2.csv` | `ala2/measure_students.py` |
| 7 | alanine dipeptide, neural-network students | `fig10_students*.csv` | `ala2/measure_students.py` |
| 8 | Li<sub>3</sub>OCl students at 1000 K | `fig12_li_1000.csv` | `li3ocl/analyze_hops.py`, `defect_states.py` |
| 9 | the channel the certificate refers to | `fig15_*.csv` | `li3ocl/equilibrium_coordinate.py`, `dV_profile.py`, `clean_channel.py`, `bracket_vs_rate.py` |
| 10 | the teacher against density-functional theory | `fig13_*.csv` | `li3ocl/teacher_vs_dft.py`, `paired_dft.py` |
| 11 | acquisition for alanine dipeptide | `fig11_*.csv` | `ala2/active_learning.py`, `al_two_sided.py`, `measure_AL.py` |
| 12 | two-channel model, ensemble of error fields | `fig4_curves_ens.csv` | `twochannel_integrated.py`, `twochannel_scan.py` |
| 13 | condition number of a diffusion coefficient | `fig7_1d.csv`, `fig7_2d*.csv` | `diffusion_condition.py` |
| 14 | opposite capacity responses | `fig16_amplitude_*.csv` | `research_extensions/response_geometry*.py` |
| 15 | finite friction | `fig6_case*.csv` | `friction_bracket.py`, `friction_crossover.py` |
| S1 | spatial arrangement and nonlinear response | `fig16_*.csv` | `research_extensions/response_geometry*.py` |
| S2 | framework of the teacher | `figS1_lattice.csv` | `li3ocl/lattice_integrity.py` |

Tables VII and S4 (Li<sub>3</sub>OCl models) are `data/li3ocl_students_1000K.csv`, written by `li3ocl/students_table.py`; Tables VIII and S3 (kinetic loss) are `data/kinetic_loss_scan_exact.csv`, written by `kinetic_loss_scan.py`. The values in Table XI come from the scripts of the corresponding figures and sections.

The programs that draw the figures are not part of this archive; the values above are what they draw.

## Reproducing

The one- and two-dimensional experiments need only NumPy and SciPy and run in minutes:

```
python doublewell_check.py
python bracket_check.py
```

Alanine dipeptide needs OpenMM 8.6 and PyTorch; Li<sub>3</sub>OCl needs MACE and PyTorch. Analyses that read
derived data only, such as

```
cd li3ocl && python bracket_vs_rate.py
```

run against `data/` with no simulation at all. The scripts that need raw trajectories or stored hop frames are
`analyze_hops.py`, `defect_states.py`, `clean_channel.py`, `dV_profile.py`, `student_side.py`,
`select_two_sided.py`, `lattice_integrity.py` and `students_table.py`, all in `li3ocl/`; regenerate the
trajectories with the notebooks in `colab/` first.

## Licence

Code (every `.py` and `.ipynb`) under the MIT Licence; data and model weights (`data/`, `figs/data/`, `*.pt`, `*.model`) under CC BY 4.0. See `LICENSE` and
`LICENSE-data`.
